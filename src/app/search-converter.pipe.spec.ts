import { SearchConverterPipe } from './search-converter.pipe';

describe('SearchConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
